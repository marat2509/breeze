---
import Layout from '../../layouts/Layout.astro';
import AuthOverlay from '../../components/auth/AuthOverlay';
import ToastContainer from '../../components/shared/Toast';
import MyConnectedAppsPage from '../../components/account/MyConnectedAppsPage';
import { resolveLocaleFromRequest } from '../../i18n/locales';
import { translate } from '../../i18n/resources';

const locale = resolveLocaleFromRequest(Astro.request);
---

<Layout title={translate(locale, 'account.connectedApps.title')} locale={locale}>
  <AuthOverlay client:load />
  <MyConnectedAppsPage client:load />
  <ToastContainer client:load />
</Layout>
